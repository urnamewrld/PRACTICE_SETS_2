class Character:
    def __init__(self, health, damage, speed):
        self.health = health 
        self.damage = damage 
        self.speed = speed 

    def take_damage(self, amount):
        self.health -= amount 

class Warrior(Character):
    def __init__(self, health, damage, speed):
        super().__init__(health, damage, speed)
        self.toughness = 0.60
    def take_damage(self, amount):
        modified_amount = amount * self.toughness 
        super().take_damage(modified_amount)

class Ninja(Character):
    def __init__(self, health, damage, speed):
        super().__init__(health, damage, speed)
        self.toughness = 0.40
    def take_damage(self, amount):
        modified_amount = amount * self.toughness 
        super().take_damage(modified_amount)

warrior = Warrior(100, 50, 60)
print(f'Initial Health: {warrior.health}')
warrior.take_damage(50)
print(f'Health after damage: {warrior.health}')
print()
ninja = Ninja(100, 40, 80)
print(f'Initial Health: {ninja.health}')
ninja.take_damage(50)
print(f'Health after damage: {ninja.health}')
